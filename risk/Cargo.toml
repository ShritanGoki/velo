[package]
name = "risk_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
wide = "1.5.0"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
libm = "0.2.16"

[[bench]]
name = "pricing_bench"
harness = false

[[bench]]
name = "fix_bench"
harness = false
